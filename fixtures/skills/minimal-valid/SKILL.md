---
name: minimal-valid
description: Minimal synthetic skill fixture used for documentation and tests.
---

# Minimal Valid Fixture

Use this fixture as the smallest valid example of a tracked skill.

## Behavior

- Contains required `name` and `description` frontmatter.
- Uses plain Markdown body content only.
- Has no bundled resources.
